






/* const baseURL = 'http://localhost:3000/resultados/listar'

const [datos, setDatos] = useState([])

function date(){

    useEffect(() => {
        axios.get(baseURL).then((response) => {
            console.log(response)
            setDatos(response.data);
        })
    })
}

const users = [
    {
        id: `${datos.codigo}`,
        name:`${datos.fecha}`,
    }
] */



export {data, statusOptions};
